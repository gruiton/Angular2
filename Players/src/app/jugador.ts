import { Component } from '@angular/core';

export class Jugador {
    id:             number;
    estado:         number;
    name:           string;
    presentacion:   string;    
    puntos:         number;
}